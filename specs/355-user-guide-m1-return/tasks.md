# Tasks — #355 slice S1 `remainder`

- [x] T355-01 Rebase `docs/355-m1-return` onto current `origin/main` per
  plan.md conflict policy; force-push with lease only this branch.
- [x] T355-02 Rewrite/amend the remaining survey per R355-02…R355-06
  (plan.md inheritance list defines done vs remaining; verify against
  the tree, including `design/registry-as-mpfs.md`).
- [x] T355-03 Fix stale refs: registry-simulator local links, theorem
  counts, nav entries, no preview URLs.
- [x] T355-04 Gate green in the worktree; push; open draft PR against
  `main` (label `docs`, assignee `paolino`); report candidate SHA.

slice: S1 covers T355-01…T355-04. No push except the issue branch. No merge.
