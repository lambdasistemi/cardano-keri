# Modules

Registry remains sole executable registry transition authority; RegistryGoals owns its public inversions and invariant statements. Cage receives only compilation adjustments forced by changed Registry interfaces. RegistryAgreement (new) owns the explicit lifecycle projection and correspondence boundary with the unchanged Checkpoint authority. RegistryLifecycleExamples (new) owns reachable Lean stories. RegistryTraceDriver receives only interface adjustments and explicit stale-simulator boundary labelling; no non-Lean regeneration.

Data constraints are in data-model.md; changed signatures in functions-model.md. No new inductive duplicate of Registry.stepFn.
