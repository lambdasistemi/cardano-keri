# Tasks: #300 — projection-fidelity requirements record

Artifact ceiling: 80 lines / 6,000 bytes.

All tasks remain unchecked until the ticket owner stamps them after audit.

- [x] T300-001 `spec.md` states the four self-contained, separately gateable
      sections R300-1 through R300-4, each citing its A-019 mandate, each
      carrying the gate-observable truths and an explicit OPEN-dependency
      line that keeps design-note decision debt open
- [x] T300-002 `plan.md` fixes the order R1 -> R2 -> R3 -> R4, the OWNER
      topology, merge predecessors, no bundling, fresh
      gate/runtime/worktree/owner/auditor per future requirement, and the
      S2 COMPLETE acceptance meaning
- [x] T300-003 `modules-model.md` carries only MOD-300-SPEC and records that
      no production module changes are authorized by #300
- [x] T300-004 `data-model.md` carries DM-300-R1 through DM-300-R4 stating
      each mandate's exact minimum and validation relationships
- [x] T300-005 `functions-model.md` records FN-300-NONE
- [x] T300-006 record identity untouched (blob, sha256, 4 OPEN / 7 settled
      tag counts), path fence held to the six owned files, ceilings and
      whitespace gates green
