# Resume brief — Cardano-KERI M1 milestone owner

## Position at 2026-08-06T12:50Z (authoritative; supersedes everything older)

You are pane `%5511` in `keri:cardano-keri-ms1-identity-core`, runtime
`/tmp/ms-keri-1`, model claude-fable-5 (operator reseat 2026-08-06 ~12:20Z of
the same pane; the 08-05 succession record and its provenance caveat stand).
One decision watcher: pid 549747 → `%5511`. Bare-load protocol: registry →
ledger.md → registry.md → session.md → this file → MEASURE children by pane.
Never restart completed work.

**OMNIA PAUSA still holds machine-wide except the e171 lane** (scoped release
06:20Z + operator direct authority 07:08Z). Only the machine owner (`%5234`)
releases anything else; silence is not permission.

## In flight RIGHT NOW

1. **#181 IS MERGED AND VERIFIED** — PR #221 MERGED 2026-08-07T04:05:31Z at head dd6b220f (merge 11a45d5c), operator-merged, guards atomic 21/21; issue CLOSED. The sole M1 stream is done.
2. **AWAITING MACHINE:** merge-SHA report + #240 scope request written (/tmp/machine/NOTE-MS1-MERGE-181-AND-240-SCOPE-2026-08-07.md) but DELIVERY PENDING — %5234 sat in a usage viewer and did not consume the pointer. RETRY FIRST. Questions in it: weekly rolled? release e156 lane for #240 only? Seat rule from #240: Codex T.O. -> Claude commit owner -> fresh Codex auditor.
3. **Merge protocol when slice 4 completes:** desk authorizes against a NAMED
   head SHA green at that SHA, undrafted; LANE runs guard-merge; **then tell
   the machine the head SHA** (its explicit ask). PR #221 head at sweep:
   `7523ecaf`, 19 SUCCESS + 2 SKIPPED, CLEAN, DRAFT.

## Queued desk work (in order)

- DISPATCHED: #257 (query-layer refactor, operator-inserted ahead of everything; e156 lane, Codex T.O., starts AT THE WEEKLY RESET 08:59 London per machine gate) then #240 consumes it. Then (Codex-seated T.O. per machine
  guidance), then the **onchain epic** — placement RULED 12:35Z: one new epic
  bundling #253 + #254 + the consumer example ("mint a CID signed by a KEL",
  ask in `/tmp/ms-keri-1/asks/`, updated header carries the ruling). Needs
  machine scope; claude budget says realistically post-weekly-roll.
- Chase BOTH epics for fresh `.orch/window-brief.md` fragments (both 08-03,
  stale) at next contact; collect into `resume/` at next sweep.
- e156 remains parked; on any future full release its #220 unpark resumes
  exactly as granted (operator 08-05), first line of that relay = its ticket
  owner's zero-model `wait-status` waker (see 08-05 brief history).
- Operator housekeeping (their funds): sweep 4991.80 tADA off the throwaway
  change address before docker volume `ckeri-qwen-demo-20260803` is deleted.

## Standing rulings that outlive any seat

- MACHINE COMMS (2026-08-07, binds every lane): NEVER inject pointers into
  %5234 — it is the operator.s live conversation. Write
  /tmp/machine/owner-opus5-takeover/inbox/NOTE-KERI-MS1-<slug>.md and send
  NOTHING; the machine sweeps every contact and reads BLOCKED from STATUS
  unaided. Pane injection ONLY for: disk hard floor, foreign kill/signal,
  live production degraded, credential exposure — or a machine gone silent on
  something that matters.

- **Outcome test has TWO halves** since 09:00Z: stranger-run packaged lifecycle
  from a PUBLISHED release (#166 audits) AND a downstream tx consuming a
  checkpoint (the consumer example; in the M1 demo).
- **Time is not the constraint — demonstrable value is** (09:04Z). 08-14 is a
  marker. Ceremony never thins for speed.
- Desk AUTHORIZES merges; owning lane runs guard-merge. Desk never merges,
  files, codes, or funds — it asks, answers, sweeps.
- Provider alternation is the seat contract; qwen/agy draft-only; provider
  exhaustion is REPORTED, never silently substituted.
- exact-PID discipline; measure seats by pane, never journals; check pane
  INPUT BOXES when a lane looks inexplicably parked.
- The cutover moves EVERY identity and is desk-gated; M1 never moves the
  compiled-UPLC proof target silently (ms8/blaster contract).
