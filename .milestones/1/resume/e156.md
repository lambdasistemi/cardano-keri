# Window brief — epic #156, milestone M1 (`lambdasistemi/cardano-keri`)

**Refreshed 2026-08-07T08:0xZ.** Supersedes the 2026-08-03 fragment entirely.
Resurrection ledger: it must let someone rebuild this window with none of my context.

## Window

`keri:3`, named **`cardano-keri-e156-t257-query-layer`**.

| pane | role | launch |
|---|---|---|
| `%5290` | **epic #156 owner** (me) | `claude --dangerously-skip-permissions --model claude-opus-5` |
| `%5789` | **ticket owner #257** | `codex-raw --dangerously-bypass-approvals-and-sandbox -C /code/cardano-keri-257-query -c model_reasoning_effort=high` |

Bottom row is free for #257's commit owner and auditor, which it dispatches itself.

**Holding window `@3995`** (`cardano-keri-e156-t220-PARKED`) holds ticket-220's lane,
**parked and alive, deliberately not killed**: `%5514` ticket owner (`codex-raw`,
`gpt-5.6-sol` high) and `%5519` Sonnet commit owner. Context intact. Do not reuse or
close those panes without a ruling.

## Authority state — read before doing anything

- **OMNIA PAUSA (2026-08-05T15:57Z) is still in force.** Only the machine owner
  releases, on the operator's word. **Silence is not permission.**
- **This lane's exception is exactly `#257`, then `#240`. Nothing else unparks.**
  Filing #259 was separately authorized (A-017).
- Lane serial order: **#257 -> #259 -> #240**. #240 remains a desk order.

## Children

| ticket | state | where |
|---|---|---|
| **#257** query-algebra refactor | **ACTIVE** | `%5789`, worktree `/code/cardano-keri-257-query`, branch `feat/257-query-algebra`, runtime `/tmp/ms-keri-1/e156/ticket-257/` |
| **#259** flake.lock enforcement | filed, queued after #257 | not started |
| **#240** Koios off write path | queued | not started |
| **#220** hear-rotation | **PARKED**, out of M1 | `@3995`, worktree `/code/cardano-keri-220-verify`, runtime `/tmp/ms-keri-1/e156/ticket-220/` |
| **#162** | not dispatched | `drafts/ticket-162-brief.md`; re-derive its hearing-capability dependency at dispatch |

Seat chain for #257: **Codex ticket owner -> Claude Sonnet commit owner -> fresh Codex
auditor per submission.** Never two identical models.

## Contract registry

| contract | parties | enforcing check |
|---|---|---|
| every input declared in `offchain/flake.nix` is locked in committed `offchain/flake.lock` | flake author vs every gate invocation, every lane | **NONE** - commissioned as **#259** |
| query algebra enforces "OR local node OR provider, never a conjunction" | #257 vs #240 consumers | being built by #257 |
| indexer read surface (`Indexer/Query/Tx.hs`) | **e171's component** vs #257's local interpreter | build ON it; schema changes OUT of scope, escalate |
| M1 auditor obeys a nameable skill revision | desk vs every auditor seat | **pin + START-echo handshake** |

## The skill-pin handshake — mandatory for auditor seats

1. The spawning brief **pins** llm-settings revision + `SKILL.md` sha256.
2. The auditor's `START` **echoes the sha256 of the text it actually read**.
3. **Mismatch => STOP and escalate.** A dirty working copy is a mismatch by definition.

Current pin, verified three ways (revision exists, its blob hashes to this, on-disk
matches, working copy clean): `commit-auditor` -> llm-settings **`969ff86`**, sha256
**`8bfb083c657215a4246dd03af30801861612946f124a950e68a7b0222818b200`**.

**Open exposure:** seven tracked skill files in `llm-settings` are dirty, not the three
the desk named. Four are in this lane's mandated load chain - `ticket-orchestrator`,
`resolve-ticket`, `worker-protocol`, `commit-owner` - so seats below me run text that
exists in no revision. Reported upward; not mine to commit. Mitigation: every seat
records the on-disk sha256 of its whole load chain in `START`, flagging which were dirty.

## Gate facts — do not re-derive

- **Exactly one `flake.nix`, at `offchain/flake.nix`.** The root has none and none is missing.
- Authoritative gate is **`just ci` from the repository root** (NOT inside `nix develop`)
  -> `ci-onchain + ci-blake3 + ci-offchain`. Scoped: `just ci-offchain`, `just build-offchain`.
- **A component-scoped green never means the repo builds** - this epic shipped a CI-red
  slice after full acceptance for exactly that reason.
- **"Could not evaluate must be RED."** A cold-store timeout is `GATE-INCOMPLETE`, a
  *distinct* finding from `GATE-FAIL`.
- `--no-write-lock-file` already exists here, applied unevenly: **9** justfile invocations
  have it, **28** do not. #259 makes it uniform.
- Disk floors: cold 40 / warm 34 / hard 30 GiB, byte-based, on `/`. **nix auto-collects
  below 48 GiB and trims to 80 - the first cold build triggers a collection. Design, not incident.**

## Standing rules

- **Exact-PID-or-nothing** + Amendment 1. Never signal by pattern. **Cross-lane
  termination is an incident class regardless of method.**
- **Bind to `$TMUX_PANE`, never ambient focus.** An untargeted `tmux display-message -p`
  returns whichever pane the human is *looking at* - #257's owner reported itself as
  `%5290` (mine) on its first breath because of this.
- **A waker matches the TAG COLUMN, not the line:**
  `^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9:]{8}Z  (RELEASE|UNPARK|MODEL-SWAP|CHILD-START)  `
- **A wake is not an authorization.** Never resolve an ambiguous wake permissively.
- Supervise only the immediate child. Never reach into a grandchild.
- **Do not push during a pause** (push => CI => unattended compute). Local commits allowed.
- Ticket owners never edit `docs/`.

## Recorded expectation, ratified by the desk

> A worker **refusing a dirty baseline and filing**, instead of quietly restoring the
> tree - **tidiness that hides defects is the failure mode.**

## Resume

Full record: `/tmp/ms-keri-1/e156/RESUME.md`. Runtime root `/tmp/ms-keri-1/e156/`.
Parent: M1 desk `%5511` (succession from `%5237` still carried as an unconfirmed **claim**).
