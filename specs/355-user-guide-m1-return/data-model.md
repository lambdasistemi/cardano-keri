# Data model — #355

No schema, fields, relationships, validation, or state changes. The only
data-like artifact is the `mkdocs.yml` nav map, which must list exactly the
surviving pages: the R355-01 triple absent, `design/registry-as-mpfs.md`
present, both simulators as local paths.
