# Plan

One OWNER slice, alternative-family author, independent fresh Lean completeness/inversion review, then only necessary proof adaptation and final independent evidence. Candidate implementation is child-owned. Author and audit seats occupy only active-stage slots in @304; no new window, no Claude, draft=NONE. Preserve Checkpoint as read-only adopted reference.

Freeze runtime gate before dispatch; verify declared compiler executable/sysroot/import identities and subject input hashes. Start with targeted CardanoKeri baseline. Later build BOTH libraries and run Lean-only behavioral checks, exact refusal/return/conviction cases and meaningful mutants; do not run stale simulator acceptance as the cure's judge. Keep traceability/compiled-trust semantics using the pinned compiler without changing offchain flake/lock or scripts outside lean.

One repair batch, two candidate submissions maximum. Report exact head and fresh independent evidence as REVIEW-REQUESTED to M1; no merge. Draft PR stays explicit that non-Lean408 work and358 are outstanding.

M1 A-006 (ticket A-002-model-stage-gate-order) explicitly authorizes MODEL/STATEMENTS before full gate readiness: one normal alternate-family creator, capped4expensive/100cheap/45active minutes. It produces the actual interface; the full executable gate is then frozen and falsified before admitting a candidate. Independent statement/inversion acceptance precedes proofs. BaselineControl-v3 is retained; no author-owned fixture or green build is acceptance. Allocation and submission caps never reset.
