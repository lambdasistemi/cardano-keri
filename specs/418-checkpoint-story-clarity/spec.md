# Checkpoint story clarity

Readers of the checkpoint story register need to distinguish an AID's permanent leaf from its successive tokens, first registration from revival, and the retired lag economy from the adopted freeze.

| Invariant | Required outcome |
|---|---|
| INV-418-TOKEN | A fresh checkpoint token belongs to each registration or revival incarnation; the AID leaf is permanent. |
| INV-418-ENTRY | Once per AID ever bounds first registration only. Revival using a later public rotation whose current keys leaked remains an admitted risk. |
| INV-418-FREEZE | Retired pure-lag enforcement is distinct from the adopted freeze requiring a later witnessed rotation and pool below P; no guard changes. |
| INV-418-CLASS | Story 12 remains in its checked bullet, with exact clause reconciliation and separate honest attribution of first registration and revival. |
| INV-418-SCOPE | Only the three story corrections and directly corresponding classification entries change; runtime, models, scenarios, parameters and gates remain intact. |
| INV-418-BASE | Acceptance uses fresh named checks on an exact candidate descended from the actual merge of PR #390. |

Success requires all six rows, independent review, and the existing reconciliation checks. A pre-repair gate result cannot establish acceptance. No new production tests or theorem proofs are requested.
