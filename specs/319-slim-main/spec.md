# Slim the identity implementation

A maintainer needs the current build to expose the surviving checkpoint operations, without shipping the retired bounty economy or record/cursor skeleton. A downstream designer needs reproducible applied-script and transaction-fit measurements before choosing the poison encoding. Invalid evidence must remain invalid after code is lifted out of a retired module.

```mermaid
flowchart LR
  E[Retire enforcement economy] -->|preserve conviction evidence| S[Retire record skeleton]
  S -->|preserve MPF verification| M[Measure surviving family]
  M -->|publish exact candidate evidence| R[Independent review]
```

This is the machine-facing requirements register for #319 and ordered children #334, #335, #336. Authority: frozen issue bodies/discussions, ticket brief and epic answers. Historical inventory counts are provenance, not quotas. Initial base: 5a35284be464a3706df4093ebc67ea64f33e65a4. Desk A-004 and epic A-003 authorize retained migration compatibility. Executable gate admission is pending.

| Requirement | Observable success | Rejection / failure |
|---|---|---|
| REQ-319-RETIRE | Current Aiken/Haskell build omits bounty, entitlement, claim/reap settlement and skeleton operations | Removed operations remain admitted or generated |
| REQ-319-CONVICTION | Lifted binder and conviction predicate preserve positive/negative verdicts and generated cross-language parity | Stub, dead duplicate, weakened witness/signature/binding check |
| REQ-319-PRESERVE | Surviving validators/builders keep their existing authorized behavior; MPF proofs execute | Surviving behavior disappears or negative proof stops rejecting |
| REQ-319-MANIFEST | Current candidate derivation/schema/fixtures describe slim family; historical deployed evidence keeps exact source interpretation | Old live identities relabelled as new deployment or mismatch accepted |
| REQ-319-MEASURE | Committed command discovers full surviving shipped extent, applies real parameters, measures script/transaction fit and emits attributable table | Empty/truncated set, wrong arity, fabricated parameters, unknown use path represented as pass |
| REQ-319-DOCS | Current claims, links, speech and shared poison-decision size citation match final tree | Stale economy claim, stale speech, missing integration handoff |
| REQ-319-READY | Exact final audited/pushed SHA has full CI, strict docs, links and measurement receipts | Earlier SHA verdict or planning-only remote head presented as implementation |

All rows are BLOCKING for delivery. Conviction, preservation, manifest identity and ledger bounds affect chain state/signatures/value. No new Lean semantics, simulator behavior, registry repair, authorization redesign, deployment, live transaction, issue/PR comment or merge is authorized.

Compatibility decoding and modeled migration tests do not establish that immutable deployed version-zero ARMED/FROZEN checkpoints can exit or migrate. The separate #279 cutover inventory is desk-owned and does not hold this deletion. Historical live-deployment evidence is preserved; candidate measurement artifacts are explicitly undeployed.
