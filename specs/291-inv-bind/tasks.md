# Tasks

## S291-1 — Total parse and remove caller offsets

- [x] **T291-001** Freeze the compact mandate, TIER-1 invariant severities,
  RED fixture design, immutable gate, scope fence, and stopping rule.
- [ ] **T291-002** Land all eight adversarial fixtures/proofs against the
  unfixed Haskell and Aiken binders and capture intended pre-repair RED counts.
- [ ] **T291-003** Implement the shared bytes-only total decoder and Haskell
  parity mirror with keripy corpus agreement.
- [ ] **T291-004** Remove caller offsets from Registration, Advance,
  enforcement, and hash-proof interfaces and propagate every consumer/golden.
- [ ] **T291-005** Permanently run the counted single-byte framing mutation
  sweep and preserve existing authentication negatives.
- [ ] **T291-006** Run the bounded local-node malformed-envelope rejection and
  prove zero checkpoint/proof-token state delta.
- [ ] **T291-007** Obtain a fresh alternate-provider audit for full-scope
  submission 1; permit at most one report-bound delta submission and re-audit.
- [ ] **T291-008** Stamp accepted tasks, mechanically verify the audited tree,
  run the immutable gate and full CI, and hand the local commit to #274 without
  push or authenticated GitHub mutation.

## Output ceiling

This artifact is limited to 45 lines and 4 KiB.
