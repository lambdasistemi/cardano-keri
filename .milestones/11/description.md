Outcome: the decomposed record+cursor family — the M1-terminal redesign as deployable validators, delivered as usable packaged executables on the `ckeri` line.

Founded 2026-08-18 on M1's terminal evidence (steering package sha256 793bab01…d56410; project ruling: the monolithic checkpoint is NO-GO at 25,934 B = 158.3% of the transaction limit — an architectural constraint). This milestone builds the architecture that ruling mandates. Status: architectural experiment until its gates pass; the experiment claims policy is in force for every external word.

INHERITANCES, named so nothing is re-derived: the proven decoder (adversarial 16/16 RED-unfixed → GREEN-repaired in BOTH languages against never-re-versioned gate 7037228); the G1 budget map and run-sheet harness (four legs, pinned mainnet PV11: depth structurally ≤ ~27% of memory at maximum; multisig within limits to 20 keys measured; historical recovery under half budget; witness axis 24-maximum-measured, not a bound); the frozen-gate methodology; and the machine's scaffolding lesson — four cold slots died in harness, zero in subject — which gate S1 exists to prevent recurring.

THE FAMILY: per-AID append-only multi-branch RECORD (MPF spine: events keyed location+SAID carrying validator-computed post-event key-state snapshots; occupancy map; INV-BIND bytes-only admission; receipt GRADING fully-witnessed/partial/bare, never gating, never economic); validator-derived CURSOR (keys, KERI state clean/forked-recovered/duplicity-detected/irreconcilable, grade, last-moved and registration slots; moved only by KERI's superseding rules); LINEAGE lifecycle (one-shot genesis policy = lineage id; juvenility window W calibrated above measured canary latency; successor stamping by presence proof; close leaves the permanent marker — memory without verdict); MAINTENANCE ESCROW (prepaid premium per performed fully-witnessed append, to any performer; notice-period close; class invariant: NO on-chain flow whose profitability depends on another party's misbehavior); STAGED PROOF-TOKEN appends where one transaction cannot carry parse+proofs+transition (token policy = parser script; presence proves the validating path ran); CONSUMER PREDICATE LIBRARY plus the reference cursor-consumer (policy over keys+state+grade+freshness, phrased as "adopted scripts pre-commit to record-relative policy") demonstrated AUTHORIZING before and REFUSED after a witnessed conflict.

GATES, in order — the milestone is BLOCKED while any is red:

S0. SIZE, fail-fast: per-script compiled sizes of every family member measured from skeleton implementations against the 16,133 B reference ceiling and the transaction limit, headroom reported per release; any member above 80% at skeleton stage is redesigned before deep work proceeds. This is the single number M1 left open and it is measured FIRST.
S1. HARNESS QUALITY before subject work: the vector/fixture harness hardened (semantic comparison not byte-exact formatting; pinned formatter versions) and every gate demonstrated able to fail BEFORE any cold slot is spent on subject verdicts. No slot is consumed by scaffolding defects twice for the same cause.
S2. FAMILY BEHAVIOR: each validator (append, cursor, lineage, escrow, staging) behind an immutable can-fail gate on the INV-BIND five-gate pattern (parity corpus, adversarial fixtures with pre-repair can-fail runs where a pre-repair subject exists, mutation sweep, no-offset interface audit, live-boundary rejection), plus the budget rows for its worst case added to the G1 map under the same pin.
S3. G2 lineage-activation drill on preprod — juvenility, stamping, archival enumeration (#171 obligation), adversarial fresh-lineage drill — requiring the second written machine release at that point, with what the drill writes stated in the request.

PROCESS TERMS, adopted from what M1 proved and what it paid for: the cost function is stated at commissioning — VERDICT LATENCY IS THE SPEND; steering inputs report bare verdicts the moment they exist; tier discipline binds (measurement gates TIER-2 — findings annotate, never block legs; validator subject work TIER-1); the desk arms its lane-terminal beat on day one and records the mechanism so its absence is detectable; floors and cold-build serialization per the machine's standing regime.

Boundaries: production rollout, mainnet, announcement — outside. Delegation and credential state stay in M7/M1bis. The product gate (one named pilot gating real authority on the cursor; one independently operated watcher with published time-to-record) sits outside this milestone and gates graduation of the release line, exactly as M1 defined it. Scouting candidates and the PRAGMA-coupling caveat are on record in the M1 scouting deliverable.

Live state: the milestone desk publishes per its skill once founded; the M1 evidence corpus is at wiki M1-Reshape-2026-08-17 and M1-Terminal-Evidence-2026-08-18. https://github.com/lambdasistemi/cardano-keri/wiki/M11-State

