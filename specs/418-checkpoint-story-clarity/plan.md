# Delivery plan

One prose and classification slice updates the three specified statements in `simulator/M1-STORIES.md` and only directly necessary entries in `simulator/checkpoint-simulator-clauses.json`.

Prepare the complete correction while PR #390 is open. Preserve the starting source hashes and known upstream gate defect; do not repair, cherry-pick, or repeatedly execute that defect. After the epic supplies the merge release, update onto main containing the actual merge, verify ancestry, and run fresh build check, scenario reconciliation, scenario selftest, full local CI and diff checks. Commission independent review of the exact candidate before acceptance.

The governing authority is epic #318 A-002. The accepted plan's stated limits describe both first registration and stale-key resurrection. The existing registry and checkpoint guards provide source attribution, not a new proof claim. The design note and Lean comments belong to other tickets.

The existing story register needs no new diagrams, speech companions, page regeneration, presentation machinery or production tests for these corrections. Return a mandatory-check scope conflict to the epic.

Planning ceiling: 160 lines and 12 KiB total. Child packet ceiling: 100 lines and 12 KiB, excluding referenced sources. One author launch; at most two audited submissions and one adjudicated findings repair. Costs and token telemetry are unavailable unless reported by the provider. Acceptance remains blocked until the repaired-base release.
