# Resume — M1.2 milestone owner

**Current as of 2026-08-24T09:45Z.** This supersedes the 2026-08-18 succession snapshot
entirely: the lanes it described (`b-decoder`, `s2-witness`) are terminal and archived, and
every pane ID it named is void.

## Read this first

The host was restored on 2026-08-24 and **the tmux server was replaced**. Every pane ID in
any record written before that date refers to nothing. Do not try to address them; the seats
below are the live ones, each verified from its own journal.

`/tmp/ms-keri-11` is **preserved read-only recovery evidence** — the authoritative history of
everything before the restore. Read it freely; write no new bytes into it. All new events,
questions, answers, briefs and evidence go under
`/tmp/machine/session-restore-20260824/keri-m12/`.

## Identity

- Desk: session `keri-m12`, singleton window `cardano-keri-ms11-decomposed-record-cursor`,
  pane `%21`, runtime `/tmp/machine/session-restore-20260824/keri-m12/ms11-desk`.
- Launch: `claude --dangerously-skip-permissions --model 'claude-opus-5[1m]' --effort high`
- Load chain: `orchestrator-contract` → `milestone-orchestrator` → `context-compiler` →
  `worker-protocol` → `tmux-orchestrator` → `invariants` → `verification`.
- Parent: cardano-keri project owner, pane `%32`, runtime
  `/tmp/machine/session-restore-20260824/0-projects/cardano-keri`. Questions go to its
  `questions/`; answers come back in its `answers/`.
- Home repo `/code/cardano-keri`; ledger branch `milestones`, depth 1, fresh root per write.

## Your exact next action

**You are parked on `Q-MS12-004` and so is the lane. Do not restart anything.**

When the project owner rules:

1. If the gate version is authorized, the path is zero-build: preserve the generated bytes
   exactly, move the 94 accepted lines to the authorized sibling, regenerate
   `MEASURED-SOURCES-v2.tsv` **from its canonical recipe** rather than by hand, correct the one
   planning reference in `specs/m11-r1-event-key/plan.md`, and version the gate so its
   append-remeasurement check reads the new owning file — with can-fail evidence in **both**
   directions and frozen v4 retained as a defect witness, never deleted.
2. Before dispatching it, ask on the new gate the question that caught this one: *could a
   perfect candidate pass this?* Under v4 the answer is provably no.
3. Verify the lane's numbers **yourself** against the artifacts. This desk re-derives, it does
   not relay — and one lane question has already carried confabulated hashes that shared the
   true values' first eight characters. Cite every hash by reading the file, and **never
   abbreviate a hash a child may need to reproduce**; that habit is what produced them.

## Live state

| item | state |
|---|---|
| R1 candidate | `4bc8dad9b3fa39f98ba6d21987c9046623b8895f` |
| rejected/unpushed evidence | `a19aa5b2ce4d84f3a810d7f55a9a940cc777025e` — preserved, never pushed |
| frozen gate / freeze | `341294d9…` / `5aefd251…` — **unsatisfiable**, and still not to be touched without a ruling |
| build ledger | **5/5, exhausted.** No retry authority. A sixth needs the project owner |
| preserved regenerated report | `2cd2e80e…` — product of the spent build, in a deliberately dirty worktree with an empty index. **Do not revert, clean or stash it** |
| base report | `291ff052…`, 174 lines; regenerated is 80 lines |
| PR | `#305`, draft, retained as evidence at the tested head |
| `main` | `84e3b7159115e9169b57a85cbf9053b94aa889ba` |
| beat | task `bor5f6m7n`, event-firing, watching `r1-ticket-owner` only |

## Live seats — restored 2026-08-24, do not disturb below your own edge

- **R1 ticket owner** — pane `%22`, Codex (`gpt-5.6-sol`, high), worktree
  `/code/cardano-keri-ms11-r1-event-key-v2`, branch `ms11/r1-event-key-v2`, runtime
  `.../keri-m12/r1-ticket-owner`. **This is your only immediate child.**
- **Commit owner** — pane `%23`, Grok 4.6, terminal, write-idle.
- **Auditor** — pane `%24`, Codex, terminal.

`%23` and `%24` are the ticket owner's children, **not yours**. The release note listed all
three as released "through you"; that does not make them your control surface. Brief `%22`
and let it seat its own.

## The beat — arm it like this, not as recorded elsewhere

```sh
BEAT_ROOT=/tmp/machine/session-restore-20260824/keri-m12 BEAT_LANES="r1-ticket-owner" \
BEAT_STALE=900 BEAT_NEVER=600 BEAT_INTERVAL=30 /tmp/ms-keri-11/beat/lane-beat.sh
```

The script is unmodified and runs read-only from the preserved tree; it takes its root and
lanes from the environment. **Do not replay the invocation in the pause receipt** — it points
at frozen journals that can never advance, so it would be silent forever and
indistinguishable from a healthy quiet lane. That is the exact defect that leaked a live
waiter through the last pause. Preflight the pattern against a real journal for a non-zero
match before arming, and arm it as an event-firing monitor, never a background print-loop.

## Open with the project owner

- **`Q-MS12-004` — the frozen gate is unsatisfiable.** The live blocker on everything. Zero
  build. Supersedes `Q-MS12-003`, which asked for one sibling file and is insufficient; the
  project owner was told not to answer it.
- **Legs 5–7** (packaged, devshell, local-ci) have never run — behind `Q-MS12-004`.
- **`Q-MS12-001`** state page / ledger sweep — GRANTED with six fences, executed 2026-08-24.

## Standing rulings you inherit

- The report is **regenerated by the tool or not at all**; a hand-written hash is rejected on
  sight.
- **Do not touch the frozen gate.** A perfect candidate can pass it.
- The size-report provenance hash is **over-broad** (registry entry, 2026-08-24). Fix it in a
  separate ticket **after** R1 lands, never inside a repair for the candidate failing it.
- Withheld unless separately released: merge, product push, release, deploy, mainnet, product
  acceptance, runner/CI start, build beyond the ceiling, cleanup/GC, M8 contact, M1.3
  commissioning.
