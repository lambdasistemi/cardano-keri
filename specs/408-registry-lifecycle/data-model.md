# Data

Checkpoint transport states are live and tombstone. A parked registry leaf retains the abstract reached key state and no UTxO. Pending go-requests retain that same state until fold. Raw active leaves may have a checkpoint or pending go-request.

Env carries a plainly named undischarged358 close-authorization predicate and its opaque recipient parameter; concrete signed-intent verification is absent. Minimal live-bond/refund structure belongs to408. User phases and funding remain unchanged, except removal of the obsolete grace-window field. Checkpoint juvenility W is untouched.
