# Window brief — e171 (the indexer epic), refreshed 2026-08-07

Replication-grade. Everything needed to rebuild this lane from nothing.

## Window

`keri:2`, named `cardano-keri-e171-indexer` — **rename at the next
child dispatch** to `cardano-keri-e171-t<child>-<goal>`; #181 is merged.

Panes now:

| pane | role | launch |
|---|---|---|
| `%5189` | **epic owner** (this brief's author) | `claude --dangerously-skip-permissions --model claude-opus-5[1m]` |

RETIRED 2026-08-07 after #181 merged: ticket owner `%5513`, commit owner `%5493`,
and every auditor pane. The whole #181 lane is archived at
`/tmp/ms-keri-1/e171/.archived/cardano-keri-181/`. **`keri:2` is now a single
epic-owner pane** until #240 is dispatched.

**The launch command matters**: the running epic-owner process is the *bare*
command above, and `~/.claude/settings.json` pins a different model — a bare
respawn yields the wrong model. Pass the flags.

## Runtime roots

- epic: `/tmp/ms-keri-1/e171/` — `STATUS.md` (append-only journal),
  `EPIC-MAP.md` (children, contracts, invariant ledger), `RESUME.md`,
  `handoffs/`, `inbox/`, `answers/`.
- children: `/tmp/ms-keri-1/e171/cardano-keri-<issue>/`, each with its own
  `STATUS.md`, `brief.md`, `workers/`, `handoffs/`.

## State

- **#181 MERGED** 2026-08-07 at `dd6b220f` (merge commit `11a45d5`), issue
  CLOSED. 21/21 checks, merge-commit method, signatures preserved.
- Stories: #175, #176, #177 merged earlier; **#253 and #254 are stories 4 and 5,
  consumer obligations only** — both validator halves are the desk's and
  unstarted, not this lane's to begin.
- Next in serial order: **#240**, then #253 + #254, #162, #163/#164, #166, #186.
- **#240's ticket owner seats on CODEX** (standing ruling from #240 onward);
  alternation then gives Codex T.O. → Claude commit owner → fresh Codex auditor.

## Standing rules this lane runs under

- Alternation: adjacent authoritative seats never share a provider.
  `agy`/`qwen` are **draft-only**, one bounded invocation per slice, never
  owner/auditor/committer. **Secrets hard-bar drafts.**
- **Proportionality**: classify every slice MECHANICAL vs CONTRACT-CRITICAL
  *before* dispatch; ceremony follows what the slice **proves**, not its size.
- **A check is not accepted until it has been demonstrated able to fail.**
  Four defects in #181's last slice were all the same shape: a green that did
  not entail its mechanism. Two of them defeated instruments I designed.
- Merge via `mcp__merge-guard__guard-merge`, method = merge commit.
  **Rebase-and-merge forbidden.**
- Exact PID or nothing; prove the PID is yours; a self-reported PID is never a
  target. Never kill panes/windows to "pause".
- **"Could not evaluate" must be RED.** Pending CI is not green.
- Pre-push: run **exactly what the CI job runs**, read from
  `.github/workflows/ci.yml` — not a `just` aggregate, which diverges in both
  directions.

## Open, carried

- **#255** deposit value-coverage class; **#256** checker-superset guard. Both
  filed out of #181; EPIC-MAP invariant 15 stays `enforced: NONE` until #255.
- `/code/llm-settings` has uncommitted `commit-auditor` / `ticket-orchestrator`
  / `resolve-ticket` skill edits — a machine-wide rule living only in a dirty
  worktree on this host. Flagged, unclaimed.
- Cross-epic: e156's **#257** relocates `ChainAsset`/`ChainAssetUtxo` to
  `Cardano.KERI.ChainQuery` and repoints `Indexer/Board.hs`. This lane has **no
  in-flight branch**, so #257 need not hold.
