# LLM investigation notes (local only)
.llm/

# Build artifacts
/dist-newstyle/
offchain/dist-newstyle/
onchain/build/
onchain/plutus.json
site/

# Nix build result symlinks
result
result-*

# resolve-ticket live log
WIP.md

# Ticket-local immutable acceptance gate (runtime artifact)
/gate.sh

# keripy fixture regeneration venv (network-installed, not committed)
/tmp/keri-venv*
