# Data boundaries

DATA-332-DOC: Markdown page and existing speech-companion format only. Speech section keys match page headings and its source stamp binds the current Markdown hash. Reuse the repository format; no schema extension.

The note describes checkpoint fields, separate value components and registry leaf states by correspondence to the model. It creates no on-chain datum, encoding, hash algorithm, intent preimage specification beyond adopted sources, deployment parameter or simulator data.
