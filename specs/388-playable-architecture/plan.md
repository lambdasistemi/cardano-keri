# Delivery

One documentation slice: canonical short DSL sources, a teaching chapter, documentation extraction/replay checks, and CI wiring. Reuse existing simulator cores, grammar and MkDocs. The baseline shared DSL gate passes 30 scenarios / 104 checkpoint steps / 115 registry steps. Existing checkpoint declaration-accounting failure #387 is outside this slice.

Validate code blocks after rendering, not only source includes. Freeze a candidate, publish matching simulator/docs bytes and commission read-only Opus review in a detached worktree. Preserve existing unrelated worktrees. Haskell and Lean execution are outside this documentation-only slice.
