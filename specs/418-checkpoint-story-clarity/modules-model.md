# Responsibility record

The story register owns human-readable lifecycle meanings. The clause table owns classification and attribution of checked fragments. Their dependency is the existing scenario reconciliation. No new component or abstraction is introduced. See data-model.md and functions-model.md for unchanged surfaces.
