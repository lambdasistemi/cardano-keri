# Functions

Preserve authoritative Registry.stepFn(p, env, action, now, system), processBody(p, env, accumulator, request), replay and reachability interfaces, removing pause/resume vocabulary. Reap admission exposes the named358 premise; return admission uses the recorded key state. Define a total Lean lifecycle projection of a Registry system and AID, with explicit treatment of pending requests and tombstone. Give every changed public branch an exact bidirectional inversion; preserve unaffected guarantees.

Exact newly introduced helper names/signatures remain owner choices within these responsibilities and require gate-interface agreement before dispatch.
