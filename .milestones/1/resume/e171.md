# Resume brief — e171 (indexer epic), PARKED since 2026-08-11

Read this before touching anything. Two cross-epic obligations were created
while you were parked; you ADOPT them, you do not renegotiate them.

## 1. T254-003 — #254's PRODUCER contract is FROZEN. Adopt it.

**Path**: `/tmp/ms-keri-1/e274/shared/254-producer-contract-v1-FROZEN.md`
**sha256**: `a273c9bfe9c934d42379b61b34962a533f36deaed329f434cfa9fdd39aac2772`
(desk-verified against the file, not taken on report). Registry +
resolved-result surface with a no-single-address negative guarantee;
**T254-405 is explicitly NOT covered by it** — that stays a named open gate.

While you were parked, #254 froze the **release-hash-registry / resolved-result
producer contract** unilaterally, with a documented adaptation window — the
#257 pattern, authorized by the e274 epic owner and ratified here, precisely
because a parked consumer cannot negotiate and #254 could not stall on it.

**On resume**: read the frozen contract (shared handoff + sha published by
#254 — ask the desk for its location if it is not already in your inbox) and
**adapt your consumer halves to it**. Do not reopen the shape. If adoption
proves genuinely impossible rather than merely inconvenient, that is an
escalation to the desk with the specific impossibility named — not a
renegotiation.

## 2. T254-405 — you own the cutover-readiness seam, and it is a hard gate

**#254 cannot declare the preprod cutover unblocked from producer evidence
alone.** Your consumer-side acceptance — follower / query / relayer
multi-version resolution — is a hard external gate, and it is the one
dependency that holds #254 open after all its own work is green.

**You are its named owner.** Start condition: **immediately on your release**,
beside #254's S254-2/S254-3 rather than discovered at verification time.
Treat it as your first work on resume, ahead of your other queued items.

## 3. Pin mismatch on resume is EXPECTED

Your brief pins commit-auditor at `2cd5053/2084d5af`. Doctrine patch
`d344f8d` (auditor approval-bypass launch mandated; journal-quiet-after-START
is now a dispatcher-side pane-capture check) moved it to
`d344f8d/c90cb15c…`. **You will mismatch and correctly STOP — that is the pin
system working, not an incident.** Re-derive against disk and re-pin; the
desk's table is a convenience, never an authority.

## 4. Also true since you parked

- The KERI projection constitution is on `main` (#273): the chain projects the
  KEL and never originates identity state; transaction context is NOT identity
  state. Write consumer resolution against it.
- PR/issue bodies follow the template on `main` — reader-facing, every claim
  checkable from the repository, no internal process, no unopenable paths.
- Budgets are cumulative per ticket; an extension is a charged grant, never a
  reset.
- Disk floors are the real protection (40 cold / 34 warm / 30 hard) — auto-GC
  is configured but UNVERIFIED.
- #279 filed: an ARMED or FROZEN preprod v0 checkpoint has no authorized exit
  and blocks the entire cutover. It bears on your multi-version resolution
  work.
