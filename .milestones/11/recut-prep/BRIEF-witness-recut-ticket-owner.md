# WITNESS-RECUT TICKET-OWNER BRIEF — commissioned, NOT PLACED (host-blocked)

Fill at placement: pane/window IDs, runtime root confirmation. Everything
else is final. Authority: A-026 sha256
`b5aebb0f33ae6ea80c5b5758f54e5c04b34c4baa3e44c99356131c544a1e4a1e` §1
(read it in full, verify the hash). This campaign is NOT submission 3 of
the terminal ticket; no acceptance inherits by candidate identity.

# SEAT (at placement)

- Fresh Claude Opus 5 `[1m]` high ticket owner, new window
  `cardano-keri-ms11-tW-recut`, fresh runtime `/tmp/ms-keri-11/w-recut`;
  never `%6752`, its children, or their contexts. First act: post-cursor
  `START mode=TICKET-OWNER` via status-event.

# OBJECTIVE — one candidate, one audit, one submission

From verified post-revert main `ad0c99bb…` (tree `c2828872…`):

1. RELAND accepted S0 `137edef07917d493914e73e69b72839b2c833b50` FIRST,
   recording exact tree/content equivalence evidence; S0 design is not
   reopened.
2. Materialize on that ancestry, as ONE clean committed candidate:
   the witness determination + packaging repair content + CORRECTLY
   RE-DERIVED manifest digest + mandate/docs corrections + the
   proof-system hardening. See the candidate-composition manifest
   (recut-prep/COMPOSITION-witness-recut.md, hash in the desk journal)
   for the exact ingredient list, seed sources, and per-ingredient
   duties. Old commits/reports are read-only seeds; every resulting byte
   is the fresh commit owner's and is re-proved on the new candidate.
   Old numbers are hypotheses — every measurement re-derives.
3. Gate-first: author + falsify + freeze the new gate per the MANDATORY
   CONTRACT below; desk verification precedes product write. Then fresh
   Codex `gpt-5.6-sol` high commit owner (your child; you build the
   pane); after it parks, a distinct fresh Opus 5 `[1m]` high formal
   auditor. Audit FINDINGS ⇒ terminal, return upward; no in-campaign
   repair or second candidate.

# MANDATORY GATE CONTRACT (falsification must kill each, right-cause)

1. CI-S2W-C BLOCKING: every admissible GREEN prints candidate commit AND
   tree; the gate compares both to the exact accepted candidate; any
   other head/tree fails.
2. CI-S2W-D BLOCKING: refuse dirty worktree/index before ANY proof leg;
   tests, manifest derivation, tree controls, receipts all observe the
   same committed object.
3. Manifest/tree binding BLOCKING: any commit changing a measured-source
   byte carries the re-derived manifest digest IN THAT CANDIDATE; gate
   recomputes from the committed tree, fails on drift; narrowing
   MEASURED_SOURCE_PATHS to obtain green is forbidden.
4. Mandate bundle binding BLOCKING: the bundle-hash recipe is documented,
   executable, versioned; re-derived when any bound document changes;
   gate verifies the declared value from the committed tree.
5. CI-S2W-E REQUIRED: repo-root artifacts consumed by the offchain flake
   use the declared-input convention (three existing instances are the
   pattern), not `../` literals — the seed repair's `../specs`/`../scripts`
   MUST be converted; no untested survivor.
6. The free candidate/tree/cleanliness/digest prefix runs AFTER the
   candidate commit exists, then the full realizing gate on the same
   exact clean SHA/tree; both receipts required.

All inherited witness invariants remain mandatory: INLINE branch
preserved AND executed; REFERENCE determination; signed-creation-envelope
vs ledger-aggregate separation; live fee-boundary measurements
(25,599/25,600/25,601; 25,617/26,448 as hypotheses to re-derive);
ARCHIVED_RED Surface-B provenance object; then-current-main integration
with full rerun on drift.

# LAW (verbatim from standing rulings)

- Two-token interlock, exact `df -B1 --output=avail /nix/store` at
  acquire AND release; the campaign cold bar per A-019 §9 + any fresh
  machine release terms; per-leg fail-BEFORE-command predicates (wrapper
  support, not external races — the guard-race lesson is law);
  realizing-hooks binding; uncertain warmth = cold.
- NO realization, commit-owner placement, candidate, or PR until the
  machine issues a fresh host-capacity release FOR THIS CAMPAIGN
  (`HOST-BUILD-HOLD` stands; store below bar and falling at commission
  time). Warm authoring only until then.
- OPUS-CAPACITY per the A-014 precedent; grok/AGY/Qwen: no seats; grok
  pilot standing (2/3, trial-3 gradeability condition) neither gates nor
  substitutes anything here.
- Build accounting inherits append-only (1/3 spent). One candidate, one
  audit, one submission. Merge only after: full gate on the exact
  committed candidate + fresh audit PASS + all required CI terminal
  green on the same SHA + then-current-main integrated without
  unreviewed drift + guard merge/read-back binding expected head+tree.
- Worker-protocol journaling via status-event; Q-files; WIP notes; no AI
  attribution; hash every durable artifact; /tmp ages.
